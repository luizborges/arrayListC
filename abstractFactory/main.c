/**
 * @file: main.c
 * @author: Luiz Borges Jr.
 * @description: test and example file
 */

#include <stdio.h>
#include <stdlib.h>
//#include "abstractFactory.h"
#include "../arrayList/arrayList.h"
///////////////////////////////////////////////////////////////
////// interface user .h
///////////////////////////////////////////////////////////////

struct _vt_max {
	void (*func)();
};

struct max {
	struct _vt_max *_;
	void *classImpl;
};

//#define call(X,Y) X->vt->Y
//#define call(X)  X->vt->
//#define $ vt->
//#define $(X) X->vt->
// max->$(func)(); || max->_->func() || max->$->func() || max->vt->func() || call(max)func() || call(max,func)()
//                    max->v->func() || $(max)func()
///////////////////////////////////////////////////////////////
////// interface user .c
///////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////
////// class user .h
///////////////////////////////////////////////////////////////



///////////////////////////////////////////////////////////////
////// class user .c
///////////////////////////////////////////////////////////////
enum make {
	set, get
};

enum conf {
	get, set
};

///////////////////////////////////////////////////////////////
////// main
///////////////////////////////////////////////////////////////
int main() {
	char *str[2] = {"set", "get"};
	printf("str: %s\n", str[set]);
	arrayList_t list = NULL;//(arrayList_t)Abstract_Factory(ArrayList);
	printf("Test list: %p\n", list);
	return 0;
}

///////////////////////////////////////////////////////////////
////// outside 
///////////////////////////////////////////////////////////////




















